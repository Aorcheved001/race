================================================================
  Python 脚本速查表 (smart_car_race 环境)
  使用前请先激活环境: conda activate smart_car_race
================================================================

【1】串口数据采集 (serial_reader.py)
----------------------------------------------------------------
# 默认采集 COM24 (无线端口)
python scripts\serial_reader.py

# 指定端口
python scripts\serial_reader.py -p COM24

# 持续采集 60 秒
python scripts\serial_reader.py -t 60

# 保存数据到 data/ 目录
python scripts\serial_reader.py -t 60 --save

# 指定输出文件名
python scripts\serial_reader.py -t 60 --save -o data\my_test.txt

# 查看可用串口
python scripts\serial_reader.py --list

# 采集指定行数
python scripts\serial_reader.py -n 1000


【2】漂移数据分析 (analyze_drift.py)
----------------------------------------------------------------
# 分析默认文件 (baseline_01.txt)
python scripts\analyze_drift.py

# 分析指定文件
python scripts\analyze_drift.py data\static_drift_test.txt


【3】动态数据分析 (analyze_dynamic.py)
----------------------------------------------------------------
# 分析默认文件 (dynamic_p2_rotation.txt)
python scripts\analyze_dynamic.py

# 分析指定文件
python scripts\analyze_dynamic.py data\dynamic_test_phase1_static.txt


【4】磁力计校准可视化 (mag_calibration\plotter.py)
----------------------------------------------------------------
# 启动磁力计校准 GUI (默认 COM24)
python mag_calibration\plotter.py

# 指定端口
python mag_calibration\plotter.py --port COM24

# 指定波特率
python mag_calibration\plotter.py --baud 115200

# 输出校准参数到文件
python mag_calibration\plotter.py --output mag_calibration\calibration_params.h


【5】双车轨迹可视化器 (dual_car_viewer.py)
----------------------------------------------------------------
# 启动双车轨迹可视化 GUI
python scripts\dual_car_viewer.py

# 功能说明:
#   - 同时接收两个串口数据
#   - 实时显示两辆车轨迹
#   - 车头朝向根据yaw角度
#   - 可调整场地大小
#   - 可放置锥桶 (半径15cm)
#   - 滚轮缩放，中键拖动
#   - 数据收集与分析

# 操作说明:
#   左键点击画布: 添加锥桶
#   右键点击画布: 删除锥桶
#   滚轮上下: 放大/缩小视图
#   中键拖动: 平移视图


【6】轨迹查看器 (trajectory_viewer.py)
----------------------------------------------------------------
# 启动轨迹查看器
python scripts\trajectory_viewer.py


【7】AI自动调参工具 (ai_tuner.py)
----------------------------------------------------------------
# 运行自动采集+分析+建议
python scripts\ai_tuner.py

# 指定采集时间 (秒)
python scripts\ai_tuner.py -t 30

# 指定输出文件
python scripts\ai_tuner.py -o data\auto_test.txt


【8】文件同步 (sync.py)
----------------------------------------------------------------
# 预览同步 (Dry Run)
python tools\sync.py

# 强制同步 VS -> ADS
python tools\sync.py --force


【9】Git 命令速查
----------------------------------------------------------------
# 查看工作区状态
git st

# 查看完整状态信息
git status

# 查看最近提交历史
git last

# 查看所有分支列表
git br

# 切换到 main 分支
git switch main

# 创建并切换新分支
git switch -c fix/mag_yaw

# 添加所有更改
git add .

# 添加指定文件
git add code\ins\imu660.c

# 取消暂存指定文件
git unstage code\ins\imu660.c

# 提交更改
git commit -m "fix: 修复磁力计航向融合问题"

# 推送到远程分支
git push -u origin main

# 查看远程仓库
git remote -v

# 设置远程仓库地址
git remote set-url origin https://github.com/Aorcheved001/race.git

# 查看 Git 代理设置
git config --global --get http.proxy
git config --global --get https.proxy

# 设置 Git 使用 Clash 代理
git config --global http.proxy http://127.0.0.1:7890
git config --global https.proxy http://127.0.0.1:7890

# 取消 Git 代理
git config --global --unset http.proxy
git config --global --unset https.proxy

# 测试 GitHub 连接
git ls-remote origin


================================================================
  常用工作流程
================================================================

# 标准开发流程: 编码 -> 同步 -> 编译 -> 测试
python tools\sync.py --force
tools\headless_build.bat --flash-only
python scripts\serial_reader.py -t 60 --save

# 磁力计校准流程: 编译 -> 校准 GUI -> Git操作 -> 推送
tools\headless_build.bat --flash-only
python mag_calibration\plotter.py

# 双车测试流程: 启动可视化 -> 连接串口 -> 收集数据 -> 分析
python scripts\dual_car_viewer.py

# 推送更改到 GitHub
git st
git add .
git commit -m "feat: 添加新功能"
git push -u origin main

# 从远程拉取最新代码
git st
git fetch origin
git switch main
git pull --ff-only origin main

# 漂移测试流程: 编译 -> 采集60秒 -> 分析
tools\headless_build.bat --flash-only
python scripts\serial_reader.py -t 60 --save -o data\drift_test.txt
python scripts\analyze_drift.py data\drift_test.txt
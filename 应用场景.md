# 第21届智能车竞赛 - 卡丁快跑组应用场景分析

## 竞赛概述

**项目名称**: new_ins - 惯性导航系统  
**竞赛组别**: 第21届智能车竞赛卡丁快跑组  
**核心方案**: 纯惯性导航（无视觉辅助）  
**目标芯片**: Infineon AURIX TC377TP

---

## 竞赛科目要求

### 科目1：自动驾驶
**任务要求**:
- 车辆从起点出发，沿规定路线行驶
- 需完成直线、弯道、坡道等路况
- 需完成车库定位和停车

**适配性分析**:
- ? **INS航位推算**: 完全支持 - 提供连续的位置和航向估计
- ? **轨迹记录**: 已实现 `track.c` - 支持路线示教和回放
- ? **Pure Pursuit控制**: 已实现 - 跟踪记录的轨迹
- ?? **车库定位**: 需添加倒车功能（见下文计划）
- ? **视觉辅助**: 不需要 - 采用纯惯导方案

**关键实现**:
```c
// 轨迹记录
track_start_save();     // 开始记录轨迹
track_flash_push_point(); // 自动采样点
track_stop_save();      // 停止记录

// 轨迹回放
track_start_follow();   // 开始跟踪轨迹
track_stop_follow();    // 停止跟踪
```

### 科目2：战场救护 / 语音交互
**任务要求**:
- 语音识别和处理
- 按指令完成特定动作
- 可能涉及精确定位

**适配性分析**:
- ? **精确定位**: INS提供连续位姿估计
- ? **航向控制**: 可精确控制车辆朝向
- ?? **语音模块**: 需外接语音识别模块（本项目未包含）
- ?? **动作执行**: 需根据具体任务开发控制逻辑

### 科目3：如影随形 / 穿越迷宫
**任务要求**:
- 跟随引导车或穿越迷宫
- 需要实时路径规划和避障
- 可能需要视觉辅助

**适配性分析**:
- ?? **路径规划**: 当前仅有轨迹回放，无实时规划
- ? **避障**: 需要超声波/激光雷达等传感器（本项目未包含）
- ? **视觉辅助**: 纯惯导方案不支持

**重要说明**: 用户明确表示不需要视觉辅助，科目3为国赛必须项目，需评估可行性。

---

## 传感器配置

### 已集成传感器

| 传感器 | 型号 | 用途 | 接口 |
|--------|------|------|------|
| IMU | IMU963RA | 姿态和航向估计 | SPI/I2C |
| 编码器 | - | 速度和位移测量 | QEI |
| 转向角传感器 | - | 前轮转角测量 | ADC |
| GPS | 可选 | 绝对位置校准 | UART |

### 传感器数据流

```
IMU963RA (100Hz)
  ├── 加速度计 (ax, ay, az) → 6轴姿态解算
  ├── 陀螺仪 (gx, gy, gz) → 姿态更新 + 航向估计
  └── 磁力计 (mx, my, mz) → 磁航向观测

编码器 (4ms周期)
  ├── 左轮脉冲 → 左轮速度/位移
  └── 右轮脉冲 → 右轮速度/位移

转向角传感器
  └── 前轮转角 → 辅助航向估计
```

---

## INS算法实现

### 核心架构

```
6轴姿态解算器 (Kalman Filter)
  ├── 状态量: [roll, pitch, yaw]
  ├── 观测量: 加速度计重力向量
  └── 更新频率: 4ms (250Hz)

航向EKF (2-state EKF)
  ├── 状态量: [yaw, bias]
  ├── 观测量: 磁力计航向
  └── 更新频率: 4ms (250Hz)

位置推算
  ├── 输入: 编码器位移 + INS航向
  └── 输出: (x, y) 位置
```

### 关键参数

```c
// 6轴卡尔曼滤波器
kalman_6axis_q = 0.01f;     // 过程噪声协方差
kalman_6axis_r = 0.1f;      // 测量噪声协方差
kalman_6axis_T = 0.004f;    // 采样周期 (4ms)

// 航向EKF
Q_yaw = 5e-5f;              // yaw过程噪声
R_mag = 5e-4f;              // 磁力计测量噪声
N_b = 5e-7f;                // 静态bias噪声
N_b_frozen = 5e-9f;         // 动态bias噪声

// 零速检测
zupt_speed_threshold = 0.1f;    // 速度阈值 (m/s)
zupt_gyro_threshold = 0.05f;    // 角速度阈值 (rad/s)
```

### API接口

```c
// 初始化和重置
void Ins_init(void);
void Ins_reset(float x, float y, float theta);
void Ins_set_config(const INS_Config *config);

// 更新
void Ins_update(const INS_Input *input, float dt_s);

// 状态查询
const INS_State* Ins_get_state(void);
void Ins_get_attitude(float *roll, float *pitch, float *yaw);
void Ins_get_yaw_layers(float *yaw_gyro, float *yaw_mag_raw, 
                        float *yaw_mag_rel, float *yaw_ekf);
```

---

## 轨迹记录与回放

### Flash存储配置

```c
#define Track_Flash_Page_Begin      24      // 起始页
#define Track_Flash_Page_Max        24      // 总页数
#define Track_Flash_Point_Page_Max  127     // 每页点数
#define TRACK_SAMPLE_STEP           0.1f    // 采样步长 (米)

// 存储容量: 24页 × 127点 = 3048个轨迹点
// 覆盖距离: 3048点 × 0.1m = 304.8米
```

### 轨迹数据结构

```c
typedef struct {
    float x;           // X位置 (米)
    float y;           // Y位置 (米)
    float yaw;         // 航向角 (弧度)
    float speed_dir;   // 速度方向: 1.0=前进, 0.0=倒车
} Ins_follow;
```

### Pure Pursuit控制

```c
// 参数
lookahead_distance = 0.4f;  // 前视距离 (米)
wheelbase = 0.2107f;        // 轴距 (米)

// 转向角计算
steer_angle = atan2(2 * wheelbase * sin(yaw_error), 
                    lookahead_distance);
```

### API接口

```c
// 初始化
void track_init(void);

// 轨迹记录
void track_start_save(void);     // 开始记录
void track_stop_save(void);      // 停止记录
uint8 track_flash_push_point(void);  // 推送采样点

// 轨迹回放
void track_start_follow(void);   // 开始跟踪
void track_stop_follow(void);    // 停止跟踪

// 数据管理
void track_flash_read_page(uint32 page_num);
uint8 track_flash_get_point(uint32 point_index, Ins_follow* out);
void track_flash_write_cache(uint32 page_num);
```

---

## 改进计划

### 车库定位 - 倒车功能增强

**当前状态**: 
- ? 轨迹记录支持 `speed_dir` 字段（1.0=前进, 0.0=倒车）
- ? 编码器支持负速度检测
- ?? 缺少倒车控制逻辑

**实现方案**:
1. 记录阶段: 自动检测倒车状态并标记 `speed_dir`
2. 回放阶段: 根据 `speed_dir` 调整电机方向
3. 控制逻辑: 倒车时转向角反向

**预期效果**:
- 支持车库入库/出库操作
- 提高科目1的停车精度

---

## 项目能力总结

### ? 已实现功能

- 6轴姿态估计 (Kalman Filter)
- 航向融合 (2-state EKF + 磁力计)
- 位置推算 (编码器 + INS)
- 轨迹记录 (Flash存储)
- 轨迹回放 (Pure Pursuit)
- 前进/倒车状态记录

### ? 待完善功能

- 倒车控制逻辑
- 车库精确定位
- GPS融合（可选）

### ? 不支持功能

- 视觉辅助定位（用户明确不需要）
- 实时路径规划
- 避障功能

---

## 竞赛适配性评估

| 科目 | 适配程度 | 说明 |
|------|---------|------|
| 科目1: 自动驾驶 | ????? | 完全适配，需添加倒车控制 |
| 科目2: 战场救护 | ??? | 需外接语音模块 |
| 科目3: 如影随形 | ?? | 纯惯导方案支持有限 |

**建议**: 
1. 优先完善科目1功能（倒车入库）
2. 评估科目3是否可通过其他传感器（超声波）实现
3. 如必须参加科目3，考虑添加简单的避障传感器

---

## 技术文档索引

- **工具链约束**: `docs/TOOLCHAIN_GUIDE.md`
- **项目索引**: `docs/PROJECT_INDEX.md`
- **INS实现**: `docs/INS_IMPLEMENTATION_SUMMARY.md`
- **编码约束**: 必须使用 GB2312 编码 (`.c`/`.h` 文件)
- **构建约束**: 必须使用 ADS IDE 构建 (Ctrl+B)

---

## 参考资料

- 竞赛说明: https://blog.csdn.net/zhuoqingjoking97298/article/details/154697989
- AURIX TC377 技术手册
- IMU963RA 数据手册
- Pure Pursuit 算法论文

---

**最后更新**: 2025-01-XX  
**维护者**: 项目开发团队